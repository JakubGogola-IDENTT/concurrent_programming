package corporation

// worker represents corporation worker which is used like gorutine
// workerId is id of current worker
func worker(workerID int, tasks chan<- taskRequest, products <-chan product) {
	for {

	}
}
